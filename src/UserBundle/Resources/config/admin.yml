parameters:
    uneak.admin.user.admin.class: UserBundle\Admin\User

services:

    uneak.admin.user.admin:
        class: %uneak.admin.user.admin.class%
        arguments: [ user ]
        calls:
            - [ setController, [ UserBundle\Controller\AdminUserController ] ]
        tags:
            -  { name: uneak.routesmanager.route }

