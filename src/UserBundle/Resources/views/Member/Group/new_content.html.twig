{% trans_default_domain 'FOSUserBundle' %}

<form action="{{ path('member_group_new') }}" {{ form_enctype(form) }} method="POST" class="member_group_new">
    {{ form_widget(form) }}
    <div>
        <input type="submit" value="{{ 'group.new.submit'|trans }}" />
    </div>
</form>
