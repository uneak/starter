{% trans_default_domain 'FOSUserBundle' %}

<form action="{{ path('user_change_password') }}" {{ form_enctype(form) }} method="POST" class="user_change_password">
    {{ form_widget(form) }}
    <div>
        <input type="submit" value="{{ 'change_password.submit'|trans }}" />
    </div>
</form>
