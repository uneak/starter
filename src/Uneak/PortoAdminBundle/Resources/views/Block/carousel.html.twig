<div class="owl-carousel" data-plugin-carousel data-plugin-options='{{ options }}'>
    {% for item in items %}
        <div class="item spaced">
                {{ renderBlock(item) }}
        </div>
    {% endfor %}
</div>