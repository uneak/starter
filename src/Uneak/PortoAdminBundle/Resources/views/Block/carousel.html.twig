<div class="owl-carousel{{ classes }}" data-plugin-carousel data-plugin-options='{{ options | jsArray }}'>
    {% for item in items %}
        <div class="item spaced">
                {{ renderBlock(item) }}
        </div>
    {% endfor %}
</div>