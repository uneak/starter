{% trans_default_domain 'FOSUserBundle' %}

<form action="{{ path('member_registration_register') }}" {{ form_enctype(form) }} method="POST" class="member_registration_register">
    {{ form_widget(form) }}
    <div>
        <input type="submit" value="{{ 'registration.submit'|trans }}" />
    </div>
</form>
