{% trans_default_domain 'FOSUserBundle' %}

<form action="{{ path('member_group_edit', {'groupName': group_name} ) }}" {{ form_enctype(form) }} method="POST" class="member_group_edit">
    {{ form_widget(form) }}
    <div>
        <input type="submit" value="{{ 'group.edit.submit'|trans }}" />
    </div>
</form>
